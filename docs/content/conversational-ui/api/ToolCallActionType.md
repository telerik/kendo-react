---
title: ToolCallActionType
description: "Learn how to build custom functionality when working with the React Conversational UI by Kendo UI with the help of the ToolCallActionType."
api_reference: true
type: inner_api
slug: api_conversational-ui_toolcallactiontype
---

# ToolCallActionType
The action a user took in response to a `ToolCall` awaiting approval.

- `'approve'` — the user approved the tool invocation.
- `'reject'` — the user rejected the tool invocation.

`type` ToolCallActionType = "approve" | "reject";
