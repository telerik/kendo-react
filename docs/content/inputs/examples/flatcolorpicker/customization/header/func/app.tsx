import * as React from 'react'

import { FlatColorPicker } from '@progress/kendo-react-inputs';

const App = () => {
    return <FlatColorPicker showClearButton={false} showPreview={false} />;
}

export default App;
