import * as React from 'react';

import { TextArea } from '@progress/kendo-react-inputs';

const App = () => {
    return (
      <TextArea
        autoSize={true}
        rows={2}
        />
    );
}

export default App;
